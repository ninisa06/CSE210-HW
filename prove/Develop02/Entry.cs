using System;
using System.IO;
namespace ReadWriteFiles
{

    public class Entry
    {
        try
        {
            StreamReader reader = new StreamReader();

        }
        catch (System.Exception)
        {
            
            throw;
        }
    }
}